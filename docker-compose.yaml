services:
  good-morning-app:
    build: .
    volumes:
      #- .dockervenv:/src/.venv
      - .:/src
      - /src/.venv
    ports:
      - 8000:8000
    environment:
     - WATCHFILES_FORCE_POLLING=true